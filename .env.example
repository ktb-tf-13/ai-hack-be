# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_RELOAD=true

# Model Configuration
MODEL_PATH=models/simple_model.pt
MODEL_DEVICE=cpu

# Data Storage Configuration
# Options: csv, database
DB_TYPE=csv
CSV_DATA_PATH=temp_table

# Database Configuration (for Docker Compose & DB Connection)
DB_USER=user
DB_PASSWORD=password
DB_NAME=myapp
