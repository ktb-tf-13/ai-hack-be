version: "3.8"

services:
  backend:
    build: .
    container_name: backend-app
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    # 프로덕션에서는 외부 DB를 사용하므로 depends_on 및 links 없음
    # DB 접속 정보는 .env 파일이나 환경변수로 주입받음
